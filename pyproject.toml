[project]
name = "sha256"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "alive-progress>=3.3.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.16.0",
]
